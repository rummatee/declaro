[package]
name = "focusable-macro"
version = "0.1.0"
authors = ["Florian Schunk <florian.git@repatr.de>"]
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
